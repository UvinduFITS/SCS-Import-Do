# Client environment (Vite). These are PUBLIC — never put secrets here.
# Base URL for the API. Leave empty to use the dev proxy ("/api").
VITE_API_BASE_URL=

# Where the Vite dev proxy forwards /api during `npm run dev`.
VITE_API_PROXY=http://localhost:4000
