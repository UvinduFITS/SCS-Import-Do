# Branding assets

Drop your company logo here as **`logo.png`** (recommended height ~40px).
The header and favicon reference `/logo.png`; if it is missing, the app falls
back to `logo.svg` and then to a text badge — so nothing breaks before you add it.
